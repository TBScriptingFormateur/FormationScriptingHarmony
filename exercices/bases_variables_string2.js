function string_2(){ 
    var nodes = selection.selectedNodes()
    for( var index in nodes)
    MessageBox.information("<3 "+ nodes[index])
    //MessageBox.information("Bravo! vous avez sélectionné "+ nodes.length +" nodes")
    //MessageBox.information("<3"+nodes.join("\n <3"))
    
}
   
