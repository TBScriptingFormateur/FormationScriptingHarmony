function fixTheHouse() {
    var SelectedPegs = selection.selectedNode()
    if (node.type(SelectedPegs) != "PEG")
        MessageLog.trace("Ce node n'est pas un peg")
}
    else {
        for (var i in SelectedPegs) {
            
         }

}